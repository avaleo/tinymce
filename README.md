# TinyMCE

Switch branch to `feature/4.9.11-base` - this is the branch created from the newest 4.x.x tag. In Nexus we use TinyMCE
in version 4.9.11, so all changes must be done on top of this version.
